import org.apache.tools.ant.filters.*

processResources {
    filesMatching('**/*.properties') {
        filter ReplaceTokens, tokens: [
                "application.version": scmVersion.version
        ]
    }
}